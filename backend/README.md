# Backend

FastAPI + SQLModel backend for MV Traders.

- Authentication (JWT)
- Role-based access control
- Organizations, Users, Products, Units, Orders, Order Items, Documents, Invoices

Run locally:
```
uvicorn main:app --reload
```
